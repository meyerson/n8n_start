# Remote (Cloud Run + Postgres) example env
# Use the same N8N_ENCRYPTION_KEY value as local
N8N_PROTOCOL=https
N8N_HOST=your.domain
N8N_EDITOR_BASE_URL=https://your.domain
WEBHOOK_URL=https://your.domain
GENERIC_TIMEZONE=UTC
N8N_ENCRYPTION_KEY=change-me-strong-random-key

# Postgres connection (running on VM)
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=postgres.vm.internal
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n
DB_POSTGRESDB_PASSWORD=change-me
# Uncomment if using SSL
# DB_POSTGRESDB_SSL=true

# Runners (external mode; Cloud Run listens, external runners connect)
# Note: You will usually run runners on another service/VM in the same VPC.
N8N_RUNNERS_ENABLED=true
N8N_RUNNERS_MODE=external
N8N_RUNNERS_BROKER_LISTEN_ADDRESS=0.0.0.0
N8N_RUNNERS_BROKER_PORT=5679
N8N_RUNNERS_AUTH_TOKEN=change-me-runner-token
