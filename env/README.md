# Environment files

This folder contains example environment templates (e.g. `.env.postgres.local.example`, `.env.postgres.remote.example`).

Usage and setup steps are documented in the top-level [README.md](../README.md) under **Environment Setup**.
